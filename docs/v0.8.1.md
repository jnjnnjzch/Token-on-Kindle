# v0.8.1

- DeepSeek keeps V4 Flash and V4 Pro total Token, uncached input, cached input, output, cost, and cache rate in every source combination.
- Kindle layout uses content priorities instead of equal-height cards.
- Windows background refresh checks native focus state: an actively used source window is synchronized without reloading, while an inactive source WebView is hidden before and after refresh.
- The source-page “隐藏窗口” action hides only that source window and does not show or focus the dashboard.
- The repository keeps one reusable Build and release workflow; merged work branches are removed by the main-branch cleanup job.
