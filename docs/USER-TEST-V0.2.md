# v0.2 用户验证清单

1. 启动应用，确认主界面出现新的灰白卡片式布局。
2. 分别打开 Codex 与 DeepSeek，完成登录后点击“隐藏窗口”。
3. 在主界面点击“立即刷新用量”，等待约 5 秒。
4. 检查 DeepSeek JSON 是否包含 `models.flash`、`models.pro`、`todayCost`、`todayTokens` 和 `cacheRate`。
5. 检查 Kindle 预览：底部 80 像素应为纯黑区域。
6. 保持应用在托盘运行，十分钟后检查采集时间是否更新。
7. 在 Kindle 上确认系统白色“滑动以解锁”落在黑色安全区内。
