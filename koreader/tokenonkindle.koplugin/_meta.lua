local _ = require("gettext")

return {
    fullname = _("Token on Kindle"),
    description = _("Keeps the Token on Kindle dashboard as KOReader's sleep screen and controls the optional Kindle RTC sleep-refresh helper."),
}
