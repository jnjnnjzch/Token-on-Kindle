{
  "name": "token-on-kindle",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "Cross-platform Tauri app that turns Codex and DeepSeek web usage into a Kindle screensaver.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node --check web/app.js && node --check web/kindle-renderer.js && node --check web/extractor-base.js && node --check shared/deepseek-response-parser-v2.mjs && node --check shared/deepseek-summary-parser.mjs && node --check web/update.js",
    "smoke": "node tools/smoke-server.mjs",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
