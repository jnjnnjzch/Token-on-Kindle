{
  "name": "token-on-kindle",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Cross-platform Tauri app that turns Codex and DeepSeek web usage into a Kindle screensaver.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node --check web/app.js && node --check web/extractor.js && node --check web/update.js && node --check web/profiles.js",
    "smoke": "node tools/smoke-server.mjs",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
